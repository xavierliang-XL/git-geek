name_list = ["david","xavier","alan","jim"]
def poplist(name_list):
    """
    this method is to remove 1 element from the list and store it with another variable.
    name_list:the list
    """
    name=name_list.pop(0)
    return(name)