age=int(input("plz input your age:"))#str
def ifBasic(age):
    """
    this method is to judge if user's inputted age greater than 16.
    age:age
    """
    if age>=16:#if filter int float
        return "adult,you can enjoy your happy hour!"

print(ifBasic(age))
